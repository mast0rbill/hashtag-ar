import { AvBaseNode, AvBaseNodeProps } from './aardvark_base_node';
import { EndpointAddr } from '@aardvarkxr/aardvark-shared';
interface AvPokerProps extends AvBaseNodeProps {
    /** This callback is called when the highlight state of the poker changes because
     * of its proximity to a panel.
     */
    updateHighlight?: (shouldHighlight: boolean, panelId: EndpointAddr) => void;
}
/** Defines a node that interacts with panels like a mouse cursor. */
export declare class AvPoker extends AvBaseNode<AvPokerProps, {}> {
    m_lastActivePanel: EndpointAddr;
    m_mouseDown: boolean;
    m_lastX: number;
    m_lastY: number;
    m_lastHighlight: boolean;
    m_skipInitialMouseDown: boolean;
    buildNode(): import("@aardvarkxr/aardvark-shared").AvNode;
    private sendMouseLeave;
    private sendMouseEnter;
    private sendMouseMove;
    private sendMouseDown;
    private sendMouseUp;
    private proximityUpdate;
}
export {};
//# sourceMappingURL=aardvark_poker.d.ts.map